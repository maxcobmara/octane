%td
 .btn-group.dropdown
  %button.btn.dropdown-toggle{"data-toggle" => "dropdown"}
   .span.caret
  %ul.dropdown-menu.pull-right.action_menu
   %li= link_to content_tag(:li, fa_icon("eye", text: (t 'actions.show'))), fuel_budget
   - if can? :update, :fuel_budgets
    %li= link_to content_tag(:li, fa_icon("pencil", text: (t 'actions.edit'))), edit_fuel_budget_path(fuel_budget)
   - if can? :delete, :fuel_budgets
    %li.danger= link_to content_tag(:li, fa_icon("trash-o", text: (t 'actions.delete'))), fuel_budget_path(fuel_budget), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }
      