- provide(:title, 'New Fuel Budget')
- model_class = FuelBudget
.page-header
  %h1=  t 'fuel_budgets.new', :default => [:'helpers.titles.new', 'New %{model}'], :model => model_class.model_name.human
= render 'form'

= link_to 'Back', fuel_budgets_path
