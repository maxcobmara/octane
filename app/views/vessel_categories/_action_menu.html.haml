%td
 .btn-group.dropdown
  %button.btn.dropdown-toggle{"data-toggle" => "dropdown"}
   .span.caret
  %ul.dropdown-menu.pull-right.action_menu
   %li= link_to content_tag(:li, fa_icon("eye", text: (t 'actions.show'))), vessel_category
   - if can? :manage, :vessel_categories
    %li= link_to content_tag(:li, fa_icon("pencil", text: (t 'actions.edit'))), edit_vessel_category_path(vessel_category)
    - if vessel_category.valid_for_removal==true
     %li.danger= link_to content_tag(:li, fa_icon("trash-o", text: (t 'actions.delete'))), vessel_category_path(vessel_category), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }